services:
  file-gator:
    image: filegator/filegator:v7.15.1
    container_name: file-gator
    networks:
      - dockarr
    volumes:
      - ${DOWNLOADS}:/var/www/filegator/repository
    restart: always
    ports:
      - "8080:8080"
networks:
  dockarr:
    external: true